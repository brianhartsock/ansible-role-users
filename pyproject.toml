[project]
name = "ansible-role-users"
version = "0.0.0"
requires-python = ">=3.14"

[dependency-groups]
dev = [
  "molecule",
  "molecule-plugins[docker]",
  "pytest-testinfra",
  "ansible",
  "ansible-lint",
  "flake8",
  "pre-commit",
]
